;;----------------------------------------------------------------------------
;; swiper conf
;;----------------------------------------------------------------------------

;; swiper
(require-package 'swiper)
(load "swiper-autoloads")
(global-set-key (kbd "C-s") 'swiper)

(provide 'init-swiper)
